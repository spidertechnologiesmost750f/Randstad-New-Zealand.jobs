# Local server only (START-SERVER.bat). Vercel uses static hosting + mailto submit.
PORT=3000

# Optional Vercel env vars (Project → Settings → Environment Variables)
AGENCY_EMAIL=workschengenhr@gmail.com
AGENCY_NAME=Randstad New Zealand

# Optional — only if you add serverless email later (not required on Vercel)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
